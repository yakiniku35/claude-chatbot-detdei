# API 策略建議：免費額度之後怎麼辦

> 對象：這個專案（DEI 評估助手）的維護者
> 更新日期：2026-09-19
> 文中 Anthropic 價格為 2026-06 的官方牌價快照，其他供應商為概略定位。
> **實際請一律以各家官網的定價頁為準**（價格變動很頻繁）。

---

## 一、先講結論

| 你的情況 | 建議做法 |
|---|---|
| 目前：自己用、同學用、Demo 給老師看 | **繼續用 Groq 免費層**，但要做好降級與錯誤處理（本次已完成） |
| 下一步：給系上/社團 10～50 人試用 | 升級到 **Groq 付費層**（改一個環境變數即可，程式不用動） |
| 再下一步：真的有公司或單位在用 | 換成 **有 SLA 的商用 API**（Anthropic / OpenAI / Google），並加上自架的用量控管 |
| 有機敏資料不能外傳 | **自架開源模型**（Ollama / vLLM），但要準備 GPU 與維運人力 |

一句話：**免費額度適合「驗證想法」，不適合「承諾服務」。**
只要你開始對任何人說「這個工具可以用」，就該進入付費層。

---

## 二、這次程式碼做了哪些優化

### API 呼叫

| 項目 | 原本 | 現在 | 效果 |
|---|---|---|---|
| 模型降級 | 寫死單一模型，額度滿就整個掛掉 | `MODEL_CHAIN` 四層自動降級 | 主力模型額度用完仍可服務 |
| 對話歷史 | 每次送出**全部**歷史 | 只送最近 10 則 + 12000 字元上限 | 長對話省約 **75% token** |
| 開場白 | 也會被送進 API | 自動排除 | 每次省約 200 token |
| 回應方式 | 等整段生成完才顯示 | **串流輸出**（逐字顯示） | 使用者感受的等待時間大幅下降 |
| 錯誤處理 | 用字串比對，分類粗糙 | `classify_error()` 依 HTTP 狀態碼分八類 | 各類對應不同動作，見下方表格 |
| 重試 | 無 | 指數退避（1.5s → 3s → 6s） | 網路抖動不再直接失敗 |
| 客戶端 | 每次 rerun 都重新建立 | `@st.cache_resource` | 減少連線建立成本 |
| 搜尋結果 | 每次都重新查 | 快取 1 小時 | 省搜尋配額 |
| `max_tokens` | 2500 | 1600 | 輸出 token 是輸入的 4～5 倍價，這裡最省錢 |

### 介面

- 自訂 CSS 對話泡泡，長篇審查報告更好讀（深色／淺色主題都相容）
- 首次進入顯示 3 個範例問題，降低「不知道要問什麼」的門檻
- 右上角「清除」按鈕，不必開側邊欄
- 手機版留白調整
- 標題下方加上「請勿輸入個資或未公開機密文件」提醒
- 主色由預設紅色改為深藍，較符合合規／稽核工具的調性
- 移除回覆後多餘的 `st.rerun()`（原本每則訊息會多渲染整頁一次）

### 順手修掉的 Bug

原本 `class AgentState` 在模組最上層引用 `add_messages`，但那個名稱只有在 `langchain` 安裝成功時才存在。
**只要部署環境沒裝 langgraph，整個 App 會在啟動時就 crash。** 已改成條件式定義。

---

## 三、為什麼免費 API 不是長久之計

### 1. 額度是「別人說了算」

Groq 免費層有每分鐘請求數（RPM）、每分鐘 token 數（TPM）、每日 token 數（TPD）三層限制。
這些數字 Groq 隨時可以調整，而且**不會事先通知你**。
最新數字請看：<https://console.groq.com/docs/rate-limits>

實際感受是：平常都好好的，結果 Demo 當天早上被別人用掉額度，你就開天窗。

### 2. 模型會突然下架

Groq 過去已下架過多個模型（例如 `llama-3.1-70b-versatile`、`mixtral-8x7b-32768`）。
原本 `app.py` 的 fallback 清單裡就有好幾個已經不存在的模型 ID。
免費服務沒有義務維持向後相容。

### 3. 沒有 SLA，壞了你只能等

免費層不保證可用性、不保證延遲、出事沒有客服。
你可以接受自己的作業掛掉一小時，但公司的合規查詢系統不行。

### 4. 資料處理條款通常對免費用戶較寬鬆

免費層常保留「用你的資料改善模型」的權利。
**這個專案處理的是公司內部公告、政策草稿、徵才文案** —— 有些本身就是未公開資訊。
這是最該優先解決的問題，比省錢重要。

> 已做：介面標題下方已加上「請勿輸入個資或未公開的機密文件」提醒。
> 還要做：確認供應商的資料處理條款，必要時升級到有 DPA 的付費層。

---

## 四、三階段升級路線

### 階段 A：還在免費層（現在）— 目標是「撐久一點」

- [x] 模型降級鏈
- [x] 歷史裁切
- [x] `max_tokens` 調降
- [ ] **加上 Prompt 快取**：`prompt.md` 有 7000+ 字元，每次呼叫都重送。
      Groq 目前對 prompt caching 的支援有限，但換到 Anthropic / OpenAI 後這一項可省 **80～90% 輸入成本**。
- [ ] **加上簡易頻率限制**：用 `st.session_state` 記錄每個 session 的呼叫次數與時間戳，
      超過門檻就擋下來，避免單一使用者把當日額度用光。
- [ ] **記錄用量**：把每次呼叫的 `response.usage`（輸入／輸出 token）寫進 log 或 Supabase。
      **沒有用量數據，後面所有成本估算都是猜的。** 這是升級前最重要的一步。

### 階段 B：付費但低成本 — 目標是「可以承諾服務」

最小改動路線：**留在 Groq，改開付費層（Developer Tier）**。
程式一行都不用改，只要帳號加上付款方式，rate limit 就大幅放寬。

如果要換供應商，`init_groq()` 和 `request_stream()` 已經把 API 呼叫集中在同一處，替換範圍很小。

### 階段 C：正式營運 — 目標是「出事有人負責」

- 後端與前端分離（FastAPI + Streamlit，或直接換前端框架）
- API 金鑰放在 Secret Manager，不放 `secrets.toml`
- 每個使用者有身分與用量配額
- 保留審計軌跡（誰在什麼時候問了什麼、系統回了什麼）
  —— 合規工具尤其需要這個
- 簽訂 DPA（資料處理協議），確認資料不被用於訓練

---

## 五、方案比較

### 商用 API

| 供應商 | 代表模型 | 輸入 $/1M tokens | 輸出 $/1M tokens | 適合這個專案嗎 |
|---|---|---|---|---|
| **Anthropic** | Claude Haiku 4.5 (`claude-haiku-4-5`) | $1.00 | $5.00 | ✅ 便宜、夠用、長文件處理穩 |
| **Anthropic** | Claude Sonnet 5 (`claude-sonnet-5`) | $2.00 | $10.00 | ✅ 品質與成本平衡點 |
| **Anthropic** | Claude Opus 5 (`claude-opus-5`) | $5.00 | $25.00 | 判斷細膩的合規審查可用 |
| **Groq 付費層** | `openai/gpt-oss-120b` 等 | 依模型而定，通常低於上表 | 同左 | ✅ **改動最小**，速度最快 |
| **Google** | Gemini 系列 | 依型號差異大 | 同左 | 有免費層可再撐一段，條款要細看 |
| **OpenAI** | GPT 系列 | 依型號差異大 | 同左 | 生態最大，但這個專案沒有非它不可的理由 |

> Anthropic 價格為 2026-06 的官方牌價快照，本文其餘內容更新於 2026-09。
> 下一節的成本試算即以這份 2026-06 的數字為基礎；實際簽約前請一律以官網定價頁為準。
> 其他家請查各自官網定價頁。
> 三家都支援 **prompt caching**，對這個專案（固定 7000 字元 system prompt）特別划算。

### 自架開源模型

| 方案 | 成本 | 優點 | 缺點 |
|---|---|---|---|
| **Ollama**（本機） | 一台電腦 | 資料不出門、零 API 費用 | 需要顯卡；品質低於商用模型；不能多人同時用 |
| **vLLM**（雲端 GPU） | 每月數千～數萬台幣 | 資料可控、高吞吐 | 要會 Linux／GPU／部署；固定成本，用不用都在燒 |

**什麼時候才值得自架？**
當「月 API 費用 > GPU 租金 + 你的維運時間成本」，或是**法規／資安規定資料不能出境**。
以這個專案目前的規模，自架幾乎一定比較貴 —— 因為你的時間也是成本。

---

## 六、怎麼估自己要花多少錢

先把用量記下來（階段 A 最後一項），然後算：

```
單次對話成本 = (輸入 tokens ÷ 1,000,000 × 輸入單價)
             + (輸出 tokens ÷ 1,000,000 × 輸出單價)
```

用這個專案的實際參數粗估（以 Claude Haiku 4.5，$1 / $5 為例）：

```
輸入：system prompt 約 2,500 tokens + 裁切後歷史約 1,500 tokens = 4,000 tokens
輸出：約 600 tokens

單次成本 = 4,000 / 1,000,000 × $1  +  600 / 1,000,000 × $5
        = $0.004 + $0.003
        = $0.007（約台幣 0.22 元）
```

| 使用量 | 月成本（美元） | 月成本（約台幣） |
|---|---|---|
| 每天 100 次對話 | $21 | 約 670 元 |
| 每天 500 次對話 | $105 | 約 3,300 元 |
| 每天 2000 次對話 | $420 | 約 13,000 元 |

**開啟 prompt caching 後**，那 2,500 tokens 的 system prompt 只算約 1/10 價，
上面的數字大約再降 **30～40%**。

> 重點：對「每天 100 次」這種規模，一個月 600 多塊台幣就能換掉所有免費層的不確定性。
> 這通常比你花時間處理「今天又掛掉了」划算得多。

---

## 七、下一步的具體待辦

依重要性排序：

1. ~~介面加上「請勿輸入機密資料」提醒~~ —— **本次已完成**
2. **記錄 `response.usage`** —— 30 分鐘，沒有它就無法決策
3. **加 session 層級頻率限制** —— 1 小時，防止單人吃光額度
4. **評估 Groq 付費層** —— 改設定即可，程式不用動
5. **導入 prompt caching** —— 換到支援的供應商後再做，效益最大

---

## 附錄：本次優化後的可調參數

全部集中在 `src/app.py` 頂端，改數字就好：

```python
MODEL_CHAIN           # 模型降級順序
MAX_HISTORY_MESSAGES  # 最多送出幾則歷史（預設 10）
MAX_HISTORY_CHARS     # 歷史字元上限（預設 12000），同時是單則訊息的截斷點
MAX_TOKENS            # 單次回覆上限（預設 2800）—— 最直接的省錢開關
                      # 注意 gpt-oss 系列的推理 token 也計入此額度，設太低會頻繁截斷
TEMPERATURE           # 預設 0.4，合規審查需要穩定輸出
MAX_RETRIES           # 重試次數（預設 3）
RETRY_BACKOFF         # 重試間隔基數（預設 1.5 秒，指數退避）
RATE_LIMIT_COOLDOWN   # 撞到 429 後暫停該模型幾秒（預設 90）
SHOW_RESPONSE_META    # 設 True 會顯示模型名稱與耗時，並顯示完整錯誤內容，除錯時很有用
```

### 錯誤分類與對應動作

`classify_error()` 把例外分成七類，各自的處理方式不同：

| 分類 | 觸發條件 | 動作 |
|---|---|---|
| `too_large` | HTTP 413 | 立即中止（換模型也沒用），提示使用者縮短輸入 |
| `rate_limit` | HTTP 429 | 該模型冷卻 90 秒，換下一個 |
| `auth` | HTTP 401／403 | 立即中止 |
| `model_gone` | HTTP 404 | 該模型本次 session 停用，換下一個 |
| `server_error` | HTTP 5xx | 退避重試，仍失敗就換下一個模型 |
| `transient` | timed out、connection | 退避重試同一個模型，仍失敗就中止（換模型也連不上） |
| `cooling` | 所有模型都在冷卻中 | 不打 API，直接告知還要等幾秒 |
| `fatal` | 其他 | 顯示通用訊息，不外洩供應商的原始錯誤內容 |

> **所有模型都冷卻時不能退回完整清單。**
> 否則每次提問都會重打一輪已限流的模型，而每個 429 又把到期時間往後推 90 秒，
> 形成永遠無法恢復的活鎖，同時白白消耗額度。

> **為什麼一定要用狀態碼判斷，不能比對訊息裡的數字？**
> Groq 對「單次請求過長」回傳 413，但錯誤碼同樣是 `rate_limit_exceeded`，
> 光看錯誤碼分不出來。而如果改用 `"413" in message` 這種裸數字比對，
> 真實的 429 訊息本身就含有 `Used 11413`、`try again in 2.413s` 這類數字，
> 會被誤判成 413 而直接中止，整條降級鏈都不會被走到。
> 因此 `http_status()` 一律優先使用 SDK 例外自帶的 `status_code`。
